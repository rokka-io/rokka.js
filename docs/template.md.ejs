<? docfiles.forEach(function(doc) { ?>

  <!-- Start <?= doc.filename ?> -->
  <? doc.javadoc.forEach(function(comment) { ?>

    <? if (!comment.ignore) { ?>
      <? if (comment.name) { ?>
          #### rokka.<?= comment.raw.ctx.string -?>
          <?= '(' + (comment.paramStr || '') + ')' -?>
          <? if (comment.returnTags.length > 0) { -?> →
            <? comment.returnTags.forEach(function(returnTag) { -?>
              <?= ' ' + returnTag.joinedTypes ?>
            <? }) ?>
          <? } ?>
      <? } ?>

        <?= comment.description ?>

        <? if (comment.deprecated) { ?>
          **Deprecated**
        <? } ?>

        <? if (comment.author) { ?>
          Author: <?- comment.author ?>
        <? } ?>

        <? if (comment.version) { ?>
          Version: <?= comment.version ?>
        <? } ?>

        <? if (comment.see) { ?>
          See: <?= comment.see ?>
        <? } ?>
    <? } ?>
  <? }) ?>

  ---

  <!-- End <?= doc.filename ?> -->

<? }) ?>
